/* Change the file name to db, so that it is used by mogoose in: 
   database/index.js */
const config = {
    host: 'myHost',
    user: 'myUser',
    pass: 'myPssword',
    port: 'myPort',
    db: 'myDatabase'
}

module.exports = config